---
interface Props {
  width: number;
  height: number;
  dir: string;
  name: string;
}

const { width, height, dir, name } = Astro.props;
---

<img src={dir} width={width} height={height} alt={`${name}のlogo`} />
